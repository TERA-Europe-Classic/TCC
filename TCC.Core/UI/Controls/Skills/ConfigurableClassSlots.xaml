<UserControl x:Class="TCC.UI.Controls.Skills.ConfigurableClassSlots"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:skills="clr-namespace:TCC.UI.Controls.Skills"
             xmlns:classManagers="clr-namespace:TCC.ViewModels.ClassManagers"
             xmlns:converters="clr-namespace:TCC.UI.Converters"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance classManagers:BaseClassLayoutViewModel}">
    <UserControl.Resources>
        <converters:ClassSkillSlotTransformConverter x:Key="ClassSkillSlotTransformConverter" />
    </UserControl.Resources>
    <ItemsControl ItemsSource="{Binding ExtraSkills}"
                  AlternationCount="9"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <Grid />
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemContainerStyle>
            <Style TargetType="ContentPresenter">
                <Setter Property="HorizontalAlignment" Value="Center" />
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="RenderTransform">
                    <Setter.Value>
                        <MultiBinding Converter="{StaticResource ClassSkillSlotTransformConverter}"
                                      ConverterParameter="Row">
                            <Binding Path="ItemsSource.Count"
                                     RelativeSource="{RelativeSource AncestorType=ItemsControl}" />
                            <Binding Path="(ItemsControl.AlternationIndex)"
                                     RelativeSource="{RelativeSource Self}" />
                        </MultiBinding>
                    </Setter.Value>
                </Setter>
            </Style>
        </ItemsControl.ItemContainerStyle>
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Grid Width="51" Height="51">
                    <Border BorderBrush="#89a"
                            BorderThickness="1"
                            Width="51"
                            Height="51"
                            Background="{StaticResource RevampBackgroundGradientBrush}"
                            RenderTransformOrigin=".5 .5"
                            Effect="{StaticResource DropShadow}"
                            Opacity=".7">
                        <FrameworkElement.RenderTransform>
                            <RotateTransform Angle="45" />
                        </FrameworkElement.RenderTransform>
                    </Border>
                    <skills:RhombSkillEffectControl />
                </Grid>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>
